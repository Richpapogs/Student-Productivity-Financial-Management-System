<Window x:Class="Library.MainWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF0F1725">

    <Window.Resources>
        <!-- Button Hover Style -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Gradient Background -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#141E30" Offset="0"/>
            <GradientStop Color="#243B55" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundGradient}">
        <Border Width="400"
                Padding="30"
                Background="#1C2433"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.4" Direction="320"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Library Borrowing and               
                           Inventory Management System"
                           Foreground="White"
                           FontWeight="SemiBold"
                           FontSize="15"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Enter your credentials to access your account"
                           Foreground="#B0B0B0"
                           FontSize="12"
                           TextAlignment="Center"
                           Margin="0,0,0,20"/>

                <TextBlock Text="Student ID No:" Foreground="White" FontSize="13"/>
                <TextBox x:Name="txtStudentId"
                         Margin="0,5,0,15"
                         Height="30"
                         FontSize="13"
                         Padding="8"
                         Background="#F5F5F5"
                         BorderThickness="0"/>

                <DockPanel>
                    <TextBlock
                               Foreground="White"
                               FontSize="13"
                               DockPanel.Dock="Left" Width="237"><Run Text="Password"/><Run Language="en-ph" Text=":"/></TextBlock>
                    <TextBlock Text="Forgot password?"
                               Foreground="#B0B0B0"
                               FontSize="12"
                               DockPanel.Dock="Right"
                               Cursor="Hand"
                               MouseDown="ForgotPassword_Click" Width="100"/>
                </DockPanel>

                <PasswordBox x:Name="txtPassword"
                             Margin="0,5,0,20"
                             Height="30"
                             FontSize="13"
                             Padding="8"
                             Background="#F5F5F5"
                             BorderThickness="0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="Create" Width="120" Click="Create_Click" Height="34"/>
                    <Button Content="Enter" Width="120" Click="Enter_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
